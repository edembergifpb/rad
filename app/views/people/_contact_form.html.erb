<div class="nested-form-wrapper" data-new-record="<%= f.object.new_record? %>">
  <%= f.label :tipo%>
  <%= f.text_field :tipo %>

  <%= f.label :valor%>
  <%= f.text_field :valor %>

  <button type="button" data-action="nested-form#remove">Remove Contato</button>

  <%= f.hidden_field :_destroy %>
</div>
